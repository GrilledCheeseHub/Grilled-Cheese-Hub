<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1600 1000" width="1600" height="1000">
  <foreignObject x="0" y="0" width="1600" height="1000">
    <html xmlns="http://www.w3.org/1999/xhtml" style="width:100%;height:100%;margin:0;padding:0;">
      <head>
        <meta charset="UTF-8"/>
        <style>
          * { margin: 0; padding: 0; }
          html, body {
            width: 100%;
            height: 100%;
            overflow: hidden;
          }
          #loadingContainer {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: #000;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            z-index: 9999;
            transition: opacity 0.5s ease-out;
          }
          #loadingContainer.hidden {
            opacity: 0;
            pointer-events: none;
          }
          .loader-content {
            text-align: center;
          }
          .loader-text {
            color: #0066cc;
            font-family: Arial, sans-serif;
            font-size: 18px;
            margin-bottom: 30px;
            font-weight: bold;
          }
          #progressBar {
            width: 400px;
            height: 40px;
            background: #1a1a1a;
            border: 2px solid #0066cc;
            border-radius: 4px;
            overflow: hidden;
            box-shadow: 0 0 10px rgba(0, 102, 204, 0.3);
          }
          #progressFill {
            height: 100%;
            background: linear-gradient(90deg, #0066cc, #00ccff);
            width: 0%;
            transition: width 0.2s ease-out;
            display: flex;
            justify-content: center;
            align-items: center;
          }
          .progress-text {
            color: #fff;
            font-family: Arial, sans-serif;
            font-size: 12px;
            font-weight: bold;
            position: absolute;
            margin-top: 10px;
            margin-left: 40px;
          }
          #gameContainer {
            width: 100%;
            height: 100%;
            border: none;
            display: block;
          }
        </style>
      </head>
      <body>
        <div id="loadingContainer">
          <div class="loader-content">
            <div class="loader-text">Loading EaglercraftX...</div>
            <div id="progressBar">
              <div id="progressFill"></div>
            </div>
            <div class="progress-text"><span id="percentText">0</span>%</div>
          </div>
        </div>
        <iframe id="gameContainer" sandbox="allow-same-origin allow-scripts allow-pointer-lock allow-topnavigation allow-forms allow-popups" allowfullscreen="true" allow="autoplay; fullscreen"></iframe>
        <script>
          (async function() {
            try {
              const progressFill = document.getElementById('progressFill');
              const percentText = document.getElementById('percentText');
              const loadingContainer = document.getElementById('loadingContainer');
              
              // Simulate initial progress
              let progress = 0;
              const progressInterval = setInterval(() => {
                if (progress < 30) {
                  progress += Math.random() * 15;
                  updateProgress(Math.min(progress, 29));
                }
              }, 100);
              
              function updateProgress(percent) {
                const clampedPercent = Math.min(Math.max(percent, 0), 100);
                progressFill.style.width = clampedPercent + '%';
                percentText.textContent = Math.floor(clampedPercent);
              }
              
              // Fetch the HTML file
              updateProgress(30);
              const response = await fetch('https://raw.githubusercontent.com/GrilledCheeseHub/Grilled-Cheese-Hub/2fb7c05ef52c0e5e454eb7c1e804560f6a15c88c/Pixel-WASM_1.12.2.html');
              updateProgress(50);
              
              if (!response.ok) {
                throw new Error('HTTP ' + response.status);
              }
              
              const htmlContent = await response.text();
              updateProgress(70);
              
              const iframe = document.getElementById('gameContainer');
              const iframeDoc = iframe.contentDocument || iframe.contentWindow.document;
              
              iframeDoc.open();
              iframeDoc.write(htmlContent);
              iframeDoc.close();
              updateProgress(90);
              
              // Wait for iframe to load
              await new Promise((resolve) => {
                iframe.onload = resolve;
                setTimeout(resolve, 2000);
              });
              
              updateProgress(100);
              clearInterval(progressInterval);
              
              // Hide loading screen
              setTimeout(() => {
                loadingContainer.classList.add('hidden');
              }, 300);
              
              // Request fullscreen on user interaction
              document.addEventListener('click', function requestFullscreen() {
                if (iframe.requestFullscreen) {
                  iframe.requestFullscreen().catch(err => console.log('Fullscreen request failed:', err));
                }
              }, { once: true });
              
            } catch (error) {
              console.error('Error loading game:', error);
              const iframe = document.getElementById('gameContainer');
              const iframeDoc = iframe.contentDocument || iframe.contentWindow.document;
              iframeDoc.open();
              iframeDoc.write('<html><body style="background:#000;color:#f00;font-family:monospace;padding:20px;"><h2>Error loading game</h2><p>' + error.message + '</p></body></html>');
              iframeDoc.close();
              document.getElementById('loadingContainer').innerHTML = '<div class="loader-content"><div class="loader-text" style="color:#f00;">Error loading game</div><p style="color:#f00;margin-top:20px;">Check console for details</p></div>';
            }
          })();
        </script>
      </body>
    </html>
  </foreignObject>
</svg>